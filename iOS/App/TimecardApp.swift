import SwiftUI

@main
struct TimecardApp: App {
    // Phase 2 will attach a SwiftData ModelContainer here (App Group backed).
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
